import { application } from 'marvel-frontend/helpers/application';
import { module, test } from 'qunit';

module('Unit | Helper | application');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = application([42]);
  assert.ok(result);
});
