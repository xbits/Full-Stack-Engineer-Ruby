import { marvelImagePath } from 'marvel-frontend/helpers/marvel-image-path';
import { module, test } from 'qunit';

module('Unit | Helper | marvel image path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = marvelImagePath([42]);
  assert.ok(result);
});
